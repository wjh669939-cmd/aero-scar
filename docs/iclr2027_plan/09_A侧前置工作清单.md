# A 侧（框架/闭环）前置工作清单与进度 v1.0

> 日期：2026-08-25　|　原则：只做不依赖 B/C/D 确认的工作；依赖项列明阻塞方，到货即接上。

---

## 一、已完成（8/25 凌晨）

`workspace/runs/aerowf-v1/` campaign 脚手架已建立，沿用 radar/sevir campaign 的治理格式：

| 产物 | 状态 | 说明 |
|---|---|---|
| `campaign_state.json` | active | preflight 阶段；discovery/certification 的解锁条件与三个待决事项（DEC-001 M轴、DEC-002 tier-2、DEC-003 ZBAD预训练）已结构化写入，每个决策带未决默认值，不阻塞冻结日 |
| `00_contract/seeds.json` | **已冻结** | 筛选 {42} / 确认 {42,2027,3407} / parent与最终 {42,2027,3407,5519,7919}，配对比较规则写死 |
| `00_contract/schemas/trial.schema.json` | **已冻结** | trial 登记 schema：假设/证据锚点/证伪条件/可编辑路径为必填；model 轴强制 param 计数字段 |
| `00_contract/schemas/result.schema.json` | **已冻结** | 三态语义（completed/invalid/failed）；hypothesis_verdict 由 DecisionPolicy 产出而非 agent 自报 |
| `00_contract/action_registry_v1_draft.json` | 草案 | 05 文档全部动作（R1–R6、O1–O6、M1–M3）机器可读化，含 conditional 状态与数据前置标注；8/31 冻结 |
| `00_contract/axis_lock_v1_draft.json` | 草案 | 锁定路径规则 + 各轴占位路径；等 D 的仓库布局填实 |
| `00_contract/decision_policy_v1_draft.json` | 草案 | **结构定死、数值 TBD**：primary endpoint、guardrail 清单、候选生命周期、反博弈规则已固定，数值等 D1 方差报告校准 |
| `00_contract/README.md` | active | 每个产物的冻结条件与阻塞方一览 |

## 二、无依赖任务进度（8/26 凌晨合并后）

**正典位置：`workspace/runs/aerowf-v1/tools/contract_tools/`（28 个用例全绿）。**
背景：曾出现两套并行实现（一套误建在已退役的 `simple_ar/aerowf_v1/`，违反 §三·五 运行时决策），8/26 凌晨已合并——取两者能力并集迁入正典位置，退役仓库副本已删除。教训：并行会话动手前先读 09 文档最新状态。

| # | 任务 | 状态 | 产出（均在 contract_tools/） |
|---|---|---|---|
| A-1 | schema 校验器（trial/result jsonschema 校验 + M 轴参数预算 gate `check_param_budget`） | **完成** | `validate.py` |
| A-2 | axis-lock 匹配引擎（allow/deny、跨轴拒绝、未激活轴拒绝、隐藏数据 token 拒绝、空 diff 拒绝、`<AEROWF_REPO>` 占位符替换） | **完成**（真实路径待 D 交付后填配置，引擎不改） | `axis_lock.py` |
| A-3 | 随机臂采样器（active 模板均匀采样、**param_space 参数采样已实体化**（registry 已补 9 个动作参数空间）、seed 可复现、conditional 自动排除、产物过 schema 校验） | **完成** | `random_arm.py` |
| A-4 | LLM 臂提案解析器 + 提示词模板 v0.1（严格 JSON、科学字段无默认值、自由提案标记、模板假设须复述） | **完成**（上下文组装器待 lineage 联通后写） | `proposal_parser.py`、`prompts/llm_proposal.md` |
| A-5 | P0 子进程隔离补丁（10 文档 §二规格）+ G3 轴配置对齐 | **完成**（8/26 凌晨，组员已获通知）：CLH 新增 `subproc.py`/`trial_runner.py`，重写 `evaluator.py`；顺带封堵 eval 标签回显漏洞；新增 objective 轴，atc 配置改 R+O 双轴；19 项测试全绿。预算 ledger 移植改挂 G1（真实 GPU 载荷）一起做 | CLH：`src/clh/research/{subproc,trial_runner,evaluator}.py`、`tests/test_subproc.py` |
| A-6 | evaluator subprocess 调用端（对齐 12 文档 CLI 合同：--predictions/--trial-meta/--split/--out-dir；三态语义、超时/崩溃/缺文件/坏 JSON 全归 failed 不误判候选） | **完成**（假 evaluator mock 联调过，真实联调等 C1） | `evaluator_client.py` |

**附带发现与修复（8/25）**：
1. **H1–H5 模块曾被误删**——git 工作区显示 20 个框架核心文件（campaign_budget、trial_reentry、finalization、parent_promotion 及其测试，共 5299 行）处于删除状态，清理记录中无有意删除记载；已从 git 恢复并重跑测试，**42 个用例全部通过**，"框架能力已验证"假设重新成立。提醒：服务器清理操作以后必须走记录。
2. schema 校验器抓出注册表草案三处不合格文本（R5/O5 证伪条件过短或"同 O4"式引用、O3 假设过短），已改为自包含的定量表述——证明冻结前先过校验器这道工序是必要的。

## 三、被阻塞项（到货即接）

| 任务 | 阻塞方 | 预计到货 |
|---|---|---|
| axis_lock 路径填实 + digest | D：修复版仓库布局 | 8/27 |
| param 计数器 + forward/backward smoke（M 轴 validator） | DEC-001 批准 + D 仓库 | 8/27 后 1–1.5 天 |
| DecisionPolicy 数值 | D1 方差报告 | 8/26 晚 |
| dataset access 层对接 | B1 manifest | 8/25 |
| evaluator 真实联调 | C1 冻结 | 8/29 |
| 禁访 token 断言注入 | C2 封存 | 8/31 前 |

## 三·五、运行时决策（8/25 增补）

组员的 CLH（`closed loop research/`）经审计后**采纳为 discovery 运行时**（见 10 文档）；radar harness 不再改造。上表 A-2/A-3/A-4/A-6 的落点相应改为 CLH 代码库；新增 P0：trial 执行子进程隔离（10 文档 §二）。

## 四、任务书变更管理规则（即日生效）

组员已阅版本为基线（06 v1.1 / 07 v1.0 / 08 v1.0）。此后任何修改：**只追加，不静默改**——改动写入各任务书末尾"变更记录"节（日期、条目、理由、影响人），重大变更另行口头同步。
