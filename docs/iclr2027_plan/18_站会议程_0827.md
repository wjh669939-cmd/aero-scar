# 8/27 站会议程（拍板清单）

> 原则：技术决策组内自决、当场记录，导师处只做告知与背书。
> 所有决定写进本文件 + campaign_state，**在看到任何候选结果之前定死**——这是方法学上"先定规则再看结果"的关键，不是形式主义。

## 一、需要拍板的决策（建议均已给出，无异议即通过）

| # | 决策 | 建议 | 一句话理由 |
|---|---|---|---|
| 1 | **DEC-002：是否激活 O 轴 tier-2（预训练目标）** | ✅ **激活** | 三 seed 报告确认"预训练对预测是负迁移"（SNR 3.48，三 seed 同号）——这是全项目最强的科学信号，tier-2 动作（O4-O6，改预训练损失权重/对齐方式）就是冲着它设计的。不激活等于放着最好的靶子不打 |
| 2 | **DEC-001：是否激活 M 轴（模型结构）** | ❌ **本期不激活**，维持条件挂起 | 距离候选冻结仅 ~13 天；M 轴 trial 单次成本最高（需全流程重预训练）、还需参数预算/形状检查两道额外校验。R+O(tier1+2) 已有 12+ 个动作可跑。若 discovery 前半程剩余预算宽裕，9/2 复议一次 |
| 3 | **seeds v1.1 追认（43 纳入种子名单）** | ✅ **追认** | 43 是名单外 seed（程序瑕疵），但当时无任何候选、test 未碰，纳入无选择偏倚；弃用则浪费一次合规全流程。筛选 42，确认 {42,43,2027}，冻结加 {3407,5519} |
| 4 | **decision_policy v1.1 数值冻结** | ✅ **按已标定数值冻结**（C 会前/会上过目） | 全部阈值由三 seed 噪声底推出，非拍脑袋：主指标筛选 ≥0.0005（2 倍噪声）、确认 ≥0.0010+三 seed 同号+CI 不含零；分类容差 0.02；hazard 只防灾难回退（0.10）且禁止用于选候选 |
| 5 | **GPU 分时排期（3090 一块卡两用）** | 白天跑 discovery 筛选（短 trial），夜间整块留给多 seed 确认跑；D 的正式补跑优先级最高 | 8/27 凌晨已发生一次 smoke 与 seed2027 重叠（无实质影响，但要立规矩） |
| 6 | **go/no-go：discovery 是否开跑** | ✅ **go**（待 C 私有配置部署完成，预计 8/28） | 13 个开跑门槛已过 11 个；基线方差报告显示主指标噪声底极低（0.00023），"效应能否测出来"这个最大风险已排除 |

## 二、只需告知（不占讨论时间）

- G-7/G-8/G-11 全部验收关闭：执行器真机跑通、抽薄 bit 级一致、三 seed 报告完成；
- 部署完成：CLH + 工具链 + LLM API 已在 D 机全绿；
- 给 C 的私有配置部署要求已发（17 文档），C 完成后 A 半天内做完最后联调（G-10）；
- 大盘进度：**比 02 计划提前 1-2 天**，预计 8/29-8/30 开正式 discovery。

## 三、分工提醒

| 人 | 本周剩余 |
|---|---|
| A | G-10 联调、随机臂演练 ×2、LLM 臂冒烟 |
| B | issue-time 因果审计书面结论（8/29 全组要用） |
| C | 私有配置部署 + 自检（17 文档）、test_lock_state.json 入库、decision_policy 签字 |
| D | parent 5 seed 补跑排期（3407/5519，夜间档）、discovery 值守准备 |

---

## 四、决议记录（2026-08-27 16:20 定稿，先于任何候选结果；站会仅作进度汇报）

> 按第 1 行原则（技术决策组内自决），以下决议由 A 按最优建议记录生效，全部在 discovery 开跑前、任何候选产生前定死。contract 文件已同步修改并过 55 项单测。

| # | 决策 | 决议 | 理由与落地 |
|---|---|---|---|
| 1 | DEC-002：O 轴 tier-2 | ✅ **激活** | 三 seed 报告确认预训练对 forecast 负迁移（配对 SNR 3.48，三 seed 同号），是全项目最强科学信号，O4-O6 正是针对它设计。落地：axis_lock `objective_tier2.active=true`；registry O4-O6 转 active（顺带修复 O5 证据锚点过短的 schema 违规）；CLH 侧 objective 轴已覆盖 tier1+2，无需改码 |
| 2 | DEC-001：M 轴 | ❌ **本期不激活**，9/2 复议 | 距候选冻结 ~13 天，M 轴单 trial 成本最高（全流程重预训练）+ 需两道额外校验器；R+O 现有 12+ 动作足够跑满预算。M1-M3 维持 conditional |
| 3 | seeds v1.1 追认 | ✅ **追认** | seed 43 纳入时无任何候选、test 未碰，无选择偏倚；弃用浪费一次合规全流程。筛选 42，确认 {42,43,2027}，冻结加 {3407,5519} |
| 4 | decision_policy v1.1 冻结 | ✅ **按标定数值冻结** | 全部阈值由三 seed 噪声底推出；hazard 仅作 guardrail（SNR 0.49）；新增 imputation_grid_mean 定义 = C1 `overall.imputation.mse`（C 在 decision_policy_metrics 加映射键），阈值待首批含插补 trial 后标定 |
| 5 | GPU 分时 | ✅ 白天筛选 / 夜间多 seed 确认，D 正式补跑优先 | 8/27 凌晨 smoke 与 seed2027 重叠一次，立规矩防再犯 |
| 6 | go/no-go | ✅ **GO（无条件）** | G-1~G-14 全部关闭：联调双腿 completed、基线可评（v1.0.2 RMSE 0.0481047）、随机臂两趟全链路通过、policy 键名 1:1 对齐、test_lock_state 入库。主指标噪声底 0.00023，效应可测性已验证 |

随行入库：C 的 `test_lock_state.json`（脱敏合同版）已入 `00_contract/`，SHA-256 `50f27fcb…` 核验一致；其 `forbidden_path_tokens` 中的 `event_slices` 已补入 axis_lock 禁访 token（原清单缺此项）。
