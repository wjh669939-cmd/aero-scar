# 8/28 晚站会材料：进度汇报 + 待确认/通知清单

> A 侧整理，2026-08-28 20:00。数据出处：lineage 统计器、trial 产物、30 号文档。

---

## 〇、重签包路径（发 C，最紧急）

| 文件 | 本机（A） | D 机 / 暂存仓 |
|---|---|---|
| v1.2 草案 | `workspace/runs/aerowf-v1/00_contract/decision_policy_v1.2_draft.json` | `clh_deploy/workspace/runs/aerowf-v1/00_contract/` 同名；暂存仓 `contract/` |
| 逐行 diff（56 行） | 同目录 `decision_policy_v1.1_to_v1.2.diff` | 同上 |
| 签字台账（已留 C 位） | 同目录 `decision_policy_v1.1_SIGNOFF.md` | 同上 |

- v1.2 SHA-256：`7268682a2c31dc9e734ef1e95bf1922d2a4f5f8bcbbe0c15ead91a9c4aa08f71`
- v1.1 原件未动（`eb49492b…` 校验一致），生效前 v1.1 继续执行。
- 给 C 的一句话：**只加 primary 的 stage 绑定（tier2 看 pretrained 腿 vs parent scratch），
  阈值/guardrail 零改动；评测器零改动，C 无需重新交付任何东西；回复
  「同意按 SHA 7268682a 冻结」即生效。** 背景见 30 号文档。

---

## 一、进度汇报（8/27 开跑至今）

### 总账（截至 8/28 20:00，trial 11 在跑）

| 项 | 数 |
|---|---|
| 有终态 trial | 8（completed 5 / failed 3） |
| 裁决 | refuted 5，not_evaluated 3，**过筛 0** |
| GPU 消耗 | ~14 h |
| 零成本拦截 | 冒烟 2 次、假自由 1 例、no-op 1 例（事后建闸） |
| 臂类别 | 全部 llm_template（暂无自由/随机臂新发） |

### 有方向感的负结果（两条）

1. **obj-008（O1 时距加权 MSE）**：主指标 **-0.000451**，距筛选线差 10%（≈2 倍噪声底），
   pretrained 腿同向微改善——目前最接近过筛的机制族；**trial 11（在跑，~21:50 收）
   就是闭环对它的参数跟进**；
2. **rep-006（R1 天气码嵌入）**：分类 scratch macro_f1 **+0.079**、hazard **+0.155**
   大幅正向，但 pretrained 端 -0.035、主指标不响应——「帮裸训分类、与预训练表示冲突」，
   与负迁移主线互证。

### parent 侧收官

- 五 seed 齐：forecast scratch **0.04825 ± 0.00017**；负迁移/正迁移五 seed 同号；
- 参照基线入表（同评测口径）：Persistence RMSE 0.0731（parent 领先 34%）、
  多数类 macro_f1 0.319 / hazard 0。**LogReg 参照待定特征口径（见 §三.4）**。

### 闭环质量证据（回答"是不是真闭环"）

- 裁决改变后续提案的实证 ×2：rep-006 避开已 refuted 的 R2 转投 R1；
  trial 11 跟进 obj-008 near-miss；
- 今日发现并当场修复三类系统缺陷，各配一道零 GPU 闸门：
  ① tier2 未接线 → 函数级拼接协议；② 假自由（obj-004 ≡ O4）→ 等价拦截；
  ③ **no-op 假施工**（obj-010 只改注释烧 2.2h，模型与 parent bit 级相同）→ AST 等价闸门。
  ③ 是 LLM 代码失败的第四类（前三类闸门拦崩溃，此类恰好"全部测试通过"），
  论文负结果账本新素材。

---

## 二、需确认（会上拍板/签字）

| # | 事项 | 决策人 | 材料 |
|---|---|---|---|
| 1 | **decision_policy v1.2 重签**（tier2 主指标 stage 绑定；修订窗口=任何候选确认前） | **C + A** | §〇路径；30 号文档 |
| 2 | 25 号文档三项追认：free_guidance 进提示词 / smoke 工程级+CHANGELOG / C 私有配置随镜像克隆 | 全组 | 25 号 §〇 |
| 3 | AIRLOCK 系统命名转正 | 全组 | 27 号文档 |
| 4 | **O3 模板处置**：只改分类损失→主指标必然 Δ=0，任何政策下不可能过筛。选项：降权 / 停用 / 另立分类赛道（动作大） | 全组 | 30 号 §六 |
| 5 | R 轴排批权重：编辑面对主指标耦合窄（两 trial forecast Δ 精确 0，只有变换 x 那条路）——名额是否向 O 轴倾斜 / 提示词是否明示 | 全组 | lineage 实证 |
| 6 | 机制去重：obj-010 复提刚被 refuted 的 O5 同族机制——是否给提案解析加"与 refuted lineage 相似度检查"（工程级，假自由拦截姊妹件） | 全组 | trial 7/10 对照 |

## 三、需通知（不占讨论时间）

1. **B**：O2 训练侧事件切片口径已定 **HAZARD-only**（29 号文档，预注册于任何 O2 trial 前）；
   B3 布尔列 + 每机场占比表随交付；issue-time 审计明天（8/29）到期；
2. **C**（重签之外）：imputation_grid_mean 映射键 + 插补 golden README 仍在清单上（非阻塞）；
3. **实验侧**：值守说明（23 号）已更新——tier2 可排、拼接协议、`tier2_function_diff.txt`
   人审位；三道新闸门语义（smoke / 假自由 / no-op 均记 `*_rejected`，零 GPU，属正常事件）；
4. **实验侧**：LogReg 参照的特征口径需一人认领（CPU 活，8/31 前）。

## 四、后续安排

| 时间 | 事项 |
|---|---|
| 今晚 ~21:50 | trial 11 收官（O1 跟进发）；**若过筛 → 明天转 3-seed 确认（GPU 整天）** |
| 今晚 | C 回签 v1.2 → A 改裁决代码 + obj-007/010 双版本回填；挂过夜批（tier1 加码 + R 轴 x-变换向，seq 12-13，视 §二.5 结论调整配比） |
| 明天（8/29） | 白天档筛选批继续（目标：累计 15+ 有终态 trial）；B 的 issue-time 审计到期；Axis Profile 骨架平移（若 §二.2 追认） |
| 8/30–31 | 视 batch 质量决定三机克隆（25 号 §五流程）；decision_policy 导师告知；D3 全部收口 |
| 9/1 | 筛选期中盘点（原目标 20+ trial）；候选池评估 |
| 9/2 | DEC-001（M 轴）复议 |
| 里程碑不变 | 9/10 候选冻结 → 9/11–12 一次性认证 → 9/18 摘要 / 9/25 全文 |

## 五、Git 状态

- 已 push：至 `v0.2.0-parent-complete`；
- **待 push：6 个 commit**（tier2 接线、no-op 闸门、O2 口径、参照基线、30 号文档、v1.2 三件套）——今晚顺手推。

---

*配套阅读：30（缺口+全轴自查）、29（O2 口径）、25（AxisProfile）、27（命名）、26（论文草稿 v0.1）。*
